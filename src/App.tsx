import TodoList from "./components/TodoList";

const App = () => {
  return (
    <section className="container w-[100vw] m-auto mt-4 flex items-center justify-center bg-green-900">
      <TodoList />
    </section>
  );
};

export default App;
